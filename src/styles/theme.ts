import {
  breakpoints,
  typography,
  spacing,
  shapes,
  layers,
  colors,
  sizes,
} from './tokens'

export default {
  colors,
  breakpoints,
  typography,
  spacing,
  shapes,
  layers,
  sizes,
} as const
