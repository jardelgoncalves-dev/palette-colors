export type Colors = {
  primary: {
    main: string
    hover: string
    active: string
  }

  neutral: {
    black: string
    white: string
  }

  grayscale: {
    paragraph: string
  }

  card: {
    background: string
    paragraph: string
    border: string
  }
}
